<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- pressed -->
    <item android:state_pressed="true" android:color="@color/secondaryColorTeal"/>
    <!-- focused -->
    <item android:state_focused="true" android:color="@color/secondaryColorTeal"/>
    <!-- activated -->
    <item android:state_activated="true" android:color="@color/secondaryColorTeal"/>
    <!-- checked -->
    <item android:state_checked="true" android:color="@color/secondaryColorTeal"/>

    <!-- pressed -->
    <item android:state_pressed="false" android:color="#979797"/>
    <!-- focused -->
    <item android:state_focused="false" android:color="#979797"/>
    <!-- activated -->
    <item android:state_activated="false" android:color="#979797"/>
    <!-- checked -->
    <item android:state_checked="false" android:color="#979797"/>

    <!-- default -->
    <item android:color="@color/black"/>

</selector>